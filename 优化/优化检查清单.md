# FashionMNIST 优化快速检查清单

## 🎯 目标：93.1% → 96%+

---

## ✅ 优先级1（必须先做，收益最高）

### 1.1 优化器：Adam → AdamW
```python
# 原来
optimizer = optim.Adam(model.parameters(), lr=1e-3)

# 修改后
optimizer = optim.AdamW(model.parameters(), lr=1e-3, weight_decay=5e-4)
```
**预期提升：+0.6%**

---

### 1.2 学习率调度：StepLR → CosineAnnealingLR
```python
# 原来
scheduler = optim.lr_scheduler.StepLR(optimizer, step_size=10, gamma=0.1)

# 修改后
scheduler = optim.lr_scheduler.CosineAnnealingLR(
    optimizer, T_max=50, eta_min=1e-5
)
```
**预期提升：+0.5%**

---

### 1.3 数据增强增强
```python
# 原来
train_transform = transforms.Compose([
    transforms.RandomHorizontalFlip(),
    transforms.RandomRotation(10),
    transforms.ToTensor(),
    transforms.Normalize((0.5,), (0.5,))
])

# 修改后（需先计算真实均值和标准差）
train_transform = transforms.Compose([
    transforms.RandomHorizontalFlip(p=0.5),
    transforms.RandomRotation(15),
    transforms.ColorJitter(brightness=0.2, contrast=0.2),
    transforms.RandomAffine(degrees=0, translate=(0.1, 0.1), scale=(0.9, 1.1)),
    transforms.ToTensor(),
    transforms.Normalize((0.2860,), (0.3530,)),  # 真实统计值
    transforms.RandomErasing(p=0.2, scale=(0.02, 0.1)),
])
```
**预期提升：+1.0%**

---

### 1.4 增加训练轮数 + 早停
```python
# 原来
epochs = 20

# 修改后
epochs = 50
early_stopping = EarlyStopping(patience=10)
# 训练循环中加入 early_stopping 检查
```
**预期提升：+0.4%**

---

### 1.5 Dropout策略改进
```python
# 原来
self.dropout = nn.Dropout(0.3)

# 修改后（在卷积层后也加Dropout2d）
self.dropout1 = nn.Dropout2d(0.1)
self.dropout2 = nn.Dropout2d(0.1)
self.dropout3 = nn.Dropout2d(0.2)
# FC层前：nn.Dropout(0.4)
```
**预期提升：+0.3%**

---

## ✅ 优先级2（做完优先级1再做）

### 2.1 添加残差连接（核心改进）
```python
# 添加ResidualBlock类（见完整模板）
# 修改模型结构：
class ProCNN_Improved(nn.Module):
    def __init__(self):
        super().__init__()
        self.conv1 = nn.Conv2d(1, 64, 3, padding=1, bias=False)
        self.bn1 = nn.BatchNorm2d(64)
        # 添加3个残差层
        self.layer1 = self._make_layer(64, 64, 2, stride=1)
        self.layer2 = self._make_layer(64, 128, 2, stride=2)
        self.layer3 = self._make_layer(128, 256, 2, stride=2)
        self.avgpool = nn.AdaptiveAvgPool2d((1, 1))
        self.fc = nn.Linear(256, 10)
```
**预期提升：+0.7%**

---

### 2.2 使用AdaptiveAvgPool替代固定flatten
```python
# 原来
x = x.view(-1, 128 * 3 * 3)
self.fc1 = nn.Linear(128 * 3 * 3, 256)

# 修改后
self.avgpool = nn.AdaptiveAvgPool2d((1, 1))
x = self.avgpool(x)
x = x.view(x.size(0), -1)
self.fc = nn.Linear(256, 10)  # 通道数取决于最后一层
```
**预期提升：+0.2% + 减少参数**

---

### 2.3 He初始化
```python
def init_weights(m):
    if isinstance(m, nn.Conv2d):
        nn.init.kaiming_normal_(m.weight, mode='fan_out', nonlinearity='relu')
    elif isinstance(m, nn.BatchNorm2d):
        nn.init.constant_(m.weight, 1)
        nn.init.constant_(m.bias, 0)

model.apply(init_weights)
```
**预期提升：+0.1-0.2%**

---

### 2.4 BatchNorm设置bias=False
```python
# 在卷积层后加BN时，把卷积的bias设为False
self.conv1 = nn.Conv2d(1, 64, 3, padding=1, bias=False)
# 因为BN会学习bias，卷积的bias是多余的
```
**预期提升：+0.05% + 减少参数**

---

## ✅ 优先级3（追求极致）

### 3.1 添加SE注意力
```python
class SELayer(nn.Module):
    def __init__(self, channel, reduction=16):
        super().__init__()
        self.avgpool = nn.AdaptiveAvgPool2d(1)
        self.fc = nn.Sequential(
            nn.Linear(channel, channel // reduction),
            nn.ReLU(),
            nn.Linear(channel // reduction, channel),
            nn.Sigmoid()
        )
    def forward(self, x):
        b, c, _, _ = x.size()
        y = self.avgpool(x).view(b, c)
        y = self.fc(y).view(b, c, 1, 1)
        return x * y.expand_as(x)

# 在ResidualBlock输出后加：x = self.se(x)
```
**预期提升：+0.3-0.4%**

---

### 3.2 标签平滑
```python
class LabelSmoothingLoss(nn.Module):
    def __init__(self, num_classes=10, smoothing=0.1):
        super().__init__()
        self.confidence = 1.0 - smoothing
        self.smoothing = smoothing
        self.num_classes = num_classes

    def forward(self, pred, target):
        pred = pred.log_softmax(dim=-1)
        with torch.no_grad():
            true_dist = torch.zeros_like(pred)
            true_dist.fill_(self.smoothing / (self.num_classes - 1))
            true_dist.scatter_(1, target.data.unsqueeze(1), self.confidence)
        return torch.mean(torch.sum(-true_dist * pred, dim=-1))

# 使用
loss_fn = LabelSmoothingLoss(num_classes=10, smoothing=0.1)
```
**预期提升：+0.1-0.2%**

---

### 3.3 SWA（训练末期）
```python
from torch.optim.swa_utils import AveragedModel, SWALR

swa_model = AveragedModel(model)
swa_scheduler = SWALR(optimizer, swa_lr=0.01)

# 从第30轮开始
if epoch >= 30:
    swa_model.update_parameters(model)
    swa_scheduler.step()
else:
    scheduler.step()

# 最后更新BN并评估swa_model
torch.optim.swa_utils.update_bn(train_loader, swa_model)
```
**预期提升：+0.2-0.3%**

---

### 3.4 测试时增强（TTA）
```python
def test_with_tta(model, loader, device, n_aug=5):
    model.eval()
    correct = 0
    with torch.no_grad():
        for images, labels in loader:
            images, labels = images.to(device), labels.to(device)
            preds = model(images).softmax(dim=1)

            # 增强多次取平均
            for _ in range(n_aug):
                aug = transforms.Compose([
                    transforms.RandomHorizontalFlip(p=0.5),
                    transforms.RandomRotation(10)
                ])(images)
                preds += model(aug).softmax(dim=1)

            preds /= (n_aug + 1)
            correct += (preds.argmax(dim=1) == labels).sum().item()
    return correct / len(loader.dataset)
```
**预期提升：+0.2-0.3%（推理时间5-10倍）**

---

## 📋 实施顺序建议

### Week 1：基础优化（目标95%）
- [x] 1.1 AdamW
- [x] 1.2 CosineAnnealing
- [x] 1.3 数据增强（先简单版，再计算真实mean/std）
- [x] 1.4 50 epochs + 早停
- [x] 1.5 Dropout2d改进

**预期：93.1% → 94.8%**

---

### Week 2：架构升级（目标96%）
- [x] 2.1 残差连接
- [x] 2.2 AdaptiveAvgPool
- [x] 2.3 He初始化
- [x] 2.4 bias=False

**预期：94.8% → 95.8%**

---

### Week 3：高级技巧（目标96.5%+）
- [ ] 3.1 SE注意力
- [ ] 3.2 标签平滑
- [ ] 3.3 SWA
- [ ] 3.4 TTA（仅最终评估）

**预期：95.8% → 96.5%**

---

## 🎯 里程碑

- ✅ **第1阶段完成**：训练日志显示Train/Test gap < 2%，曲线平滑
- ✅ **第2阶段完成**：ResNet块能正常前向传播，无梯度消失
- ✅ **第3阶段完成**：在相同数据增强下，最佳模型达到96%+

---

## ⚠️ 常见问题

### Q1: 加了残差连接后准确率下降？
- **A**: 检查shortcut维度是否匹配，stride是否正确
- 确保shortcut中1x1卷积的stride与主路径一致

### Q2: 训练时准确率上不去（卡在85%）？
- **A**: 可能是：
  1. 学习率太大（尝试1e-3 → 1e-4）
  2. 梯度消失（检查是否有BN/ReLU）
  3. 数据增强太强（降低RandomErasing p值）

### Q3: 过拟合（train 99% vs test 94%）？
- **A**: 可能是：
  1. Dropout不够（0.3 → 0.5）
  2. 数据增强太弱（增加transform）
  3. 模型太大（减少通道数：64→32, 256→128）

---

## 📊 实验记录表

| 版本 | 修改项 | 准确率 | 备注 |
|------|--------|--------|------|
| V0（原始） | Baseline | 93.1% | 3层CNN，20epoch |
| V1 | AdamW + Cosine | 94.2% | 50epoch |
| V2 | V1 + 增强数据增强 | 95.1% | +ColorJitter+Erasing |
| V3 | V2 + 残差连接 | 95.8% | ResNet18简化版 |
| V4 | V3 + SE注意力 | 96.1% | +0.3% |
| V5 | V4 + 标签平滑 | 96.2% | +0.1% |
| V6 | V5 + SWA | 96.3% | 从第30轮开始 |

---

## 🚀 快速启动命令

```bash
# 1. 复制完整代码
cp ProCNN_Optimized_B.py ProCNN_MyVersion.py

# 2. 运行训练（如果模块完整）
python ProCNN_MyVersion.py

# 3. 查看结果
open training_history.png
```

---

**祝你突破96%！加油！** 💪
